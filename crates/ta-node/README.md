# ta-node

Node integration crate for `laakhay-ta` runtime exposure.

## Status

Current crate is a minimal scaffold.

## Direction

- Keep API surface thin over `ta-engine`.
- Reuse core contracts instead of duplicating compute logic.

## Development

```bash
cargo test -p ta-node
```
