# Runtime Tools

This section covers the main production guardrails:

- `preview`: evaluate and inspect trigger points
- `validate`: catch syntax/schema/capability issues early
- `analyze`: compute required sources/timeframes/lookback

Most teams should rely on these tools in CI and deploy gates.
