# Test Matrix

Use this matrix to ensure strategy reliability before release.

## Parser / Compiler

- valid DSL expression parse coverage
- invalid syntax and unknown indicator rejection
- parameter alias and conflict handling
- source attribute chain forms

## Indicators

- window warmup behavior
- availability mask correctness
- alias and canonical name parity
- multi-output indicator wrappers

## Runtime

- evaluate single symbol + multi symbol
- preview trigger extraction
- validate/analyze consistency
- missing-source failure modes

## Pattern/Fibonacci (Recommended)

- `leg` indexing determinism
- `pairing_mode` behavior differences
- `max_leg_age_bars` invalidation
- `min_leg_size_pct` filtering
- `allow_equal_extremes` edge handling
