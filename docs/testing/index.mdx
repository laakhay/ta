# Testing & Performance

The project has broad test coverage across:

- unit tests for core, registry, primitives, indicators, DSL, planner, runtime
- integration tests for end-to-end API and multi-source expression flows
- e2e smoke tests
- performance benchmarks for evaluation hot paths

Run tests:

```bash
pytest
```
