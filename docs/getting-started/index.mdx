# Getting Started

Goal: get from raw bars to validated signal output fast.

Suggested sequence:

1. Install package.
2. Build `Dataset` from bars.
3. Run indicator/expression.
4. Use `validate`, `analyze`, and `preview`.
